import { environment } from '../../../../environments/environment';

// Export constants you want
export const Operations = {
    pending: 'pending',
    completed: 'completed',
    error: 'error',
    notStarted: 'notStarted'
};